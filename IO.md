#computer_architecture #operating_systems 

Input-Output is a general term for any device communicating with the outside world.

Generic device has status, command, and data registers. Internally some microcontroller is used to control the device based on commands.

Software wise, on [[Unix|Unix like]] systems, [[POSIX|POSIX]]  API is used to interface with some kernel module that then communicates with actual hardware.