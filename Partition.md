#computer_science #computer_architecture 

A partition is a division of single physical [[Computer_Storage|Computer Storage]] into several logical disks. Each drive must have partition table. Two types are [[MBR]] and [[GPT]].


Formatting can be done with `cfdisk` TUI or `fdisk` commands.